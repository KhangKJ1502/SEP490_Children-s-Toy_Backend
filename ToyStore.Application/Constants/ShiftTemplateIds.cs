namespace ToyStore.Application.Constants;

/// <summary>
/// Canonical shift template identifiers (see docs/database/dataseed.sql seed values).
/// </summary>
public static class ShiftTemplateIds
{
    public const byte Morning = 1;
    public const byte Evening = 3;
}
